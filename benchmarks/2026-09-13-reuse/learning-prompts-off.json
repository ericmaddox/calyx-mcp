[
  {
    "stage": 0,
    "prompt": "This is a bounded synthetic trial. Do not inspect files, run shell commands, browse, delegate, or modify files. A deterministic test has already verified this failure: Verified failure: cache_key('ABC', 'X') returned 'ABC'; expected ('X', 'ABC'). The cache key must be the tuple (venue, symbol), with venue first. Code: def cache_key(symbol, venue): return symbol. Return a concise lesson preserving the required output type and ordering. Do not repair code yet."
  },
  {
    "stage": 1,
    "prompt": "This is a bounded synthetic trial. Do not inspect files, run shell commands, browse, delegate, or modify files. Repair this function to conform to the project's previously verified cache-key convention. Previously recorded project lesson: The cache key must be a tuple (venue, symbol), with venue first: cache_key('ABC', 'X') must return ('X', 'ABC'), not 'ABC'. Code: def cache_key(symbol, venue): return symbol. Return only the corrected function as the code field. Keep its name and two parameters, use one pure return expression, and no imports, calls, decorators, or annotations."
  },
  {
    "stage": 2,
    "prompt": "This is a bounded synthetic trial. Do not inspect files, run shell commands, browse, delegate, or modify files. Repair this function to conform to the project's previously verified cache-key convention. Previously recorded project lesson: The cache key must be a tuple (venue, symbol), with venue first: cache_key('ABC', 'X') must return ('X', 'ABC'), not 'ABC'. Code: def cache_key(ticker, market): return ticker. Return only the corrected function as the code field. Keep its name and two parameters, use one pure return expression, and no imports, calls, decorators, or annotations."
  },
  {
    "stage": 3,
    "prompt": "This is a bounded synthetic trial. Do not inspect files, run shell commands, browse, delegate, or modify files. Repair this function to conform to the project's previously verified cache-key convention. Previously recorded project lesson: The cache key must be a tuple (venue, symbol), with venue first: cache_key('ABC', 'X') must return ('X', 'ABC'), not 'ABC'. Code: def lookup_key(asset, exchange): return asset. Return only the corrected function as the code field. Keep its name and two parameters, use one pure return expression, and no imports, calls, decorators, or annotations."
  }
]